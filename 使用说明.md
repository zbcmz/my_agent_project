# 04229f7 增强版 Travel Agent 使用说明

## 1. 基线与改造边界

开发基线：`xxx-x0/langgraph-trip-planner` commit `04229f7e7faa41b74cf1f7ebd655fc15beb09ad0`。

保留 baseline：LangGraph、MCP/高德、POI/Weather/Hotel 并行、景点聚类、Food/Route、Structured Output、工具/LLM 重试、timeout/fallback。

本增强层新增：

1. Supervisor 任务路由 + 并行 Specialized Workers；
2. Hard Constraint Parser；
3. Deterministic Validator；
4. Validator -> Targeted Replan 闭环，最大自动修复 2 次；
5. LangGraph Checkpointer 的 thread-level Memory；
6. SQLite user-level Long-term Preference Memory；
7. `interrupt()` + `Command(resume=...)` Human-in-the-loop；
8. 同一 thread 的自然语言局部行程修改。

## 2. 覆盖文件

把压缩包里的 `backend/` 文件复制到你 checkout 好的 `04229f7` 仓库根目录，选择合并/覆盖：

- 新增 `backend/app/agents/enhanced_langgraph_agent.py`
- 新增 `backend/app/models/agent_schemas.py`
- 新增 `backend/app/services/memory_service.py`
- 新增 `backend/app/api/routes/trip_stateful.py`
- 覆盖 `backend/app/api/main.py`（只是在原路由基础上注册增强路由）
- 新增 `backend/tests/agents/test_enhanced_validator.py`

原来的 `backend/app/agents/langgraph_agent.py` 不需要改。

## 3. 启动

和 04229f7 原项目一样配置 `.env`，然后：

```bash
cd backend
pip install -r requirements.txt
python run.py
```

打开 `http://localhost:8000/docs`，重点测试 `/api/trip-agent/*`。

## 4. 推荐 Demo 流程

### A. 创建 Stateful 行程

`POST /api/trip-agent/plan`

```json
{
  "user_id": "demo-user-001",
  "enable_human_review": true,
  "request": {
    "city": "北京",
    "start_date": "2026-09-01",
    "end_date": "2026-09-03",
    "travel_days": 3,
    "transportation": "公共交通",
    "accommodation": "经济型酒店",
    "preferences": ["历史文化"],
    "food_preference": "本地特色",
    "free_text_input": "预算3000元，带老人，不想爬山，每天最多2个景点，行程轻松一点"
  }
}
```

返回 `status=waiting_human` 和 `thread_id`，说明 Graph 已在 HITL 节点暂停。

### B. HITL 修改

`POST /api/trip-agent/resume/{thread_id}`

```json
{
  "action": "edit",
  "feedback": "第二天下午太累，删掉一个景点，第一天不要改"
}
```

流程：`interrupt -> Edit Analyzer -> Targeted Replan -> Validator -> interrupt`。

### C. 确认

再次 `POST /api/trip-agent/resume/{thread_id}`：

```json
{
  "action": "approve",
  "feedback": ""
}
```

Graph 进入 `save_memory -> END`。

### D. 已结束后再多轮修改

`POST /api/trip-agent/edit/{thread_id}`

```json
{
  "feedback": "第三天想多安排一个博物馆，其他两天不变",
  "enable_human_review": true
}
```

同一 thread 从 Checkpointer 恢复已有 TripPlan，不重新跑 POI/Weather/Hotel Research。

### E. 查看两层 Memory

- Thread Memory：`GET /api/trip-agent/thread/{thread_id}`
- User Long-term Memory：`GET /api/trip-agent/memory/demo-user-001`

## 5. 面试时要能讲清楚的实现点

- Supervisor 为什么不让所有请求固定跑相同 Agent：按请求决定 Worker，未选 Worker no-op；POI/Weather/Hotel 保持同一 LangGraph super-step 并行。
- 为什么 Validator 用 Python 而不是 LLM：预算、数量、重复项是确定性规则，程序化验证更稳定、便宜、可测试。
- 如何防止 Replan 死循环：`MAX_REPLAN = 2`，超过次数转 HITL/结束。
- 为什么是 Targeted Replan：只替换 `affected_days`，未受影响日期直接保留，减少 token、延迟和无关内容漂移。
- 两层 Memory：Checkpointer 管 thread state；SQLite Profile 管跨 thread 用户偏好。
- HITL：`interrupt()` 暂停，必须用相同 `thread_id` + `Command(resume=...)` 恢复。

## 6. 当前有意保留的简化

- Thread Checkpointer 用 `InMemorySaver`，便于本地 Demo；服务重启后 thread 会丢。面试可说明生产会替换 `AsyncPostgresSaver`。
- 长期偏好使用 SQLite，足够 Demo；生产可替换 PostgreSQL/Redis。
- 暂不加入 OR-Tools。
- Eval 先从 `test_enhanced_validator.py` 开始，后续可批量跑 20~50 个 Hard Case 统计 Constraint Pass Rate / Replan Success Rate。
